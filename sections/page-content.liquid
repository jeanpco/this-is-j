{%- liquid
  if section.settings.page.content != blank
    echo section.settings.page.content
  endif
-%}

{% schema %}
  {
    "name": "Page Content",
    "class": "page-content",
    "tag": "section",
    "settings": [
      {
        "type": "page",
        "id": "page",
        "label": "Page"
      }
    ]
  }
{% endschema %}